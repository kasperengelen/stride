How to run the demo on Data Formats:

Unzip importfies.zip, and put geogrid.json and geogrid.h5 in the instaled directory.
Put the bash files also in the installed directory. 
Put the config files (xml files) in the installed/config directory.

Run the bash scripts from the installed directory.